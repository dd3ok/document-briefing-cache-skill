# 짧은 문서 다이제스트

{% for item in summaries[:8] %}
- **{{ (item.title or item.document_id)|md }}**: {{ item.summary|md }}
{% endfor %}

{% if all_actions %}
액션: {% for action in all_actions[:3] %}{{ action.action|md }}{% if not loop.last %} / {% endif %}{% endfor %}
{% endif %}
